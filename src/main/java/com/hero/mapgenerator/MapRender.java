package com.hero.mapgenerator;

public class MapRender {
   public MapCreator map;
   public static void findPlace(MapCreator map)
   {for(int i=2;i<map.mapArray.length;i++)
       if(map.mapArray[i]==(byte)1111)
       {
           Place somePlace= Place.getPlace();

       }
   }
}
